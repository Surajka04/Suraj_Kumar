#include<iostream>
using namespace std;
#include<conio.h>
int main()
{
    int no;
    cout<<"Enter the number = ";
    cin>>no;
    if(no%2==0)
    {
        cout<<"Even no";
    }
    else
    {
        cout<<"Odd no";
    }
    getch();
    return 0;
}